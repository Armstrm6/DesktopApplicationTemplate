<Window x:Class="DesktopApplicationTemplate.UI.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:helpers="clr-namespace:DesktopApplicationTemplate.UI.Helpers"
        mc:Ignorable="d"
        Title="MainView" Width="800" Height="600"
        Style="{DynamicResource BubblyWindowStyle}" BorderBrush="LimeGreen" BorderThickness="3">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/BubblyWindow.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <helpers:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Bar with title and controls -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Column="0">
                <Button Content="+" Width="30" Height="30" Click="AddService_Click" Margin="0,0,5,0"/>
                <Button Content="-" Width="30" Height="30" Click="RemoveService_Click"/>
            </StackPanel>

            <TextBlock Grid.Column="1" Text="Chapp" FontFamily="Forte" FontSize="48" HorizontalAlignment="Center"/>

            <StackPanel Orientation="Horizontal" Grid.Column="2" HorizontalAlignment="Right">
                <Button Content="☰" Width="30" Height="30" Margin="0,0,5,0"/>
                <Button Content="—" Width="30" Height="30" Command="{x:Static SystemCommands.MinimizeWindowCommand}" Margin="0,0,5,0"/>
                <Button Content="X" Width="30" Height="30" Command="{x:Static SystemCommands.CloseWindowCommand}"/>
            </StackPanel>
        </Grid>

        <!-- Active Services display -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Left">
            <TextBlock Text="ACTIVE SERVICES:" FontWeight="Bold" Margin="0,0,10,0"/>
            <TextBlock>
                <Run Text="{Binding CurrentActiveServices}"/>
                <Run Text="/"/>
                <Run Text="{Binding ServicesCreated}"/>
            </TextBlock>
        </StackPanel>

        <!-- Log level and log management buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,20,0,0" VerticalAlignment="Center">
            <TextBlock Text="LOG LEVEL" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox x:Name="GlobalLogLevelBox" Width="120" SelectedIndex="0" SelectionChanged="GlobalLogLevelBox_SelectionChanged">
                <ComboBoxItem Content="Debug"/>
                <ComboBoxItem Content="Warning"/>
                <ComboBoxItem Content="Error"/>
                <ComboBoxItem Content="Critical"/>
            </ComboBox>
            <Button Content="Export Log" Margin="10,0,0,0" Command="{Binding ExportLogCommand}"/>
            <Button Content="Clear Log" Margin="5,0,0,0" Command="{Binding ClearLogCommand}"/>
            <Button Content="Update Log" Margin="5,0,0,0" Command="{Binding UpdateLogCommand}"/>
        </StackPanel>

        <!-- Device configuration section -->
        <StackPanel Grid.Row="3" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Device Configuration" FontSize="16" FontWeight="Bold"/>
                <TextBlock Text=" 🛠️" FontSize="16" Margin="5,0,0,0"/>
            </StackPanel>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Text="IP Address:" Margin="0,0,5,5"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NetworkConfig.CurrentConfiguration.IPAddress}" Margin="0,0,0,5"/>

                <TextBlock Grid.Row="1" Text="Subnet:" Margin="0,0,5,5"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding NetworkConfig.CurrentConfiguration.Subnet}" Margin="0,0,0,5"/>

                <TextBlock Grid.Row="2" Text="Gateway:" Margin="0,0,5,5"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding NetworkConfig.CurrentConfiguration.Gateway}" Margin="0,0,0,5"/>

                <TextBlock Grid.Row="3" Text="DNS 1:" Margin="0,0,5,5"/>
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding NetworkConfig.CurrentConfiguration.Dns1}" Margin="0,0,0,5"/>

                <TextBlock Grid.Row="4" Text="DNS 2:" Margin="0,0,5,0"/>
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding NetworkConfig.CurrentConfiguration.Dns2}"/>
            </Grid>
        </StackPanel>

        <!-- Hidden elements required by existing logic -->
        <ListBox x:Name="ServiceList" Visibility="Collapsed" />
        <Frame x:Name="ContentFrame" Visibility="Collapsed" />
    </Grid>
</Window>

