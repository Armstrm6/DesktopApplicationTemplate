<Window x:Class="DesktopApplicationTemplate.UI.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:helpers="clr-namespace:DesktopApplicationTemplate.UI.Helpers"
        mc:Ignorable="d"
        Title="MainView" Width="1200" Height="800"
        Style="{DynamicResource BubblyWindowStyle}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/BubblyWindow.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <helpers:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="#99FFFF" BorderBrush="#80FF00" BorderThickness="13" CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="160"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Bar -->
            <Border Grid.Row="0" Background="#00C2C2" CornerRadius="40" Margin="0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="TexSupport"
                               FontFamily="Forte" FontSize="72"
                               VerticalAlignment="Center" HorizontalAlignment="Center"/>

                    <Button Grid.Column="1" Content="🛠️" Width="60" Height="60" Margin="20"
                            VerticalAlignment="Center" Click="OpenSettings_Click"/>
                </Grid>
            </Border>

            <!-- Content Area -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="350"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Services Panel -->
                <Border Grid.Column="0" Background="#E6E6E6" CornerRadius="5" Margin="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="0">
                            <Button Content="☰" Width="40" Height="40" Margin="0,0,5,0" Click="OpenFilter_Click"/>
                            <Button Content="-" Width="40" Height="40" Margin="0,0,5,0" Click="RemoveService_Click"/>
                            <Button Content="+" Width="40" Height="40" Click="AddService_Click"/>
                        </StackPanel>

                        <TextBlock Grid.Row="1" Margin="10,0" Text="ACTIVE SERVICES:" FontWeight="Bold"/>

                        <ListBox x:Name="ServiceList" Grid.Row="2" Margin="10" ItemsSource="{Binding FilteredServices}"
                                 SelectedItem="{Binding SelectedService}" BorderThickness="0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 SelectionChanged="ServiceList_SelectionChanged">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="14" Background="White" Margin="5" Padding="10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="{Binding DisplayName}" FontWeight="Bold"/>
                                                <TextBlock Text="Port: {Binding Port}" FontSize="12"/>
                                                <TextBlock Text="Connected Steps: {Binding ConnectedSteps}" FontSize="12"/>
                                            </StackPanel>
                                            <ToggleButton Grid.Column="1" IsChecked="{Binding IsActive, Mode=TwoWay}"
                                                          Width="60" Height="30"
                                                          Style="{StaticResource ToggleSwitchStyle}" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <!-- Logs Panel -->
                <Border Grid.Column="1" Background="#E6E6E6" CornerRadius="5" Margin="0,20,20,20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="0" VerticalAlignment="Center">
                            <TextBlock Text="LOG LEVEL" FontWeight="Bold" VerticalAlignment="Center"/>
                            <ComboBox x:Name="GlobalLogLevelBox" Width="200" Margin="10,0,0,0"
                                      SelectionChanged="GlobalLogLevelBox_SelectionChanged" SelectedIndex="0">
                                <ComboBoxItem Content="All"/>
                                <ComboBoxItem Content="Debug"/>
                                <ComboBoxItem Content="Warning"/>
                                <ComboBoxItem Content="Error"/>
                            </ComboBox>
                        </StackPanel>

                        <ListBox Grid.Row="1" Margin="10" ItemsSource="{Binding DisplayLogs}" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Message}" Foreground="{Binding Color}"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                            <Button Content="Update Log" Width="150" Height="40" Margin="5"/>
                            <Button Content="Export Log" Width="150" Height="40" Margin="5"/>
                            <Button Content="Clear Log" Width="150" Height="40" Margin="5"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>

