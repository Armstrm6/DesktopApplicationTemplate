<Window x:Class="DesktopApplicationTemplate.UI.Views.MqttEditConnectionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:DesktopApplicationTemplate.UI.Helpers"
        Title="Edit MQTT Connection" Width="400" Height="350"
        WindowStartupLocation="CenterOwner" Style="{DynamicResource BubblyWindowStyle}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/BubblyWindow.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBox Text="{Binding Host}" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Port}" Margin="0,0,0,5"/>
            <TextBox Text="{Binding ClientId}" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Username}" Margin="0,0,0,5"/>
            <PasswordBox helpers:PasswordBoxAssistant.BindPassword="True"
                         helpers:PasswordBoxAssistant.BoundPassword="{Binding Password}" Margin="0,0,0,5"/>
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <TextBlock Text="Use TLS" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <CheckBox IsChecked="{Binding UseTls}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <TextBlock Text="Connection" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox Width="120" SelectedValuePath="Content" SelectedValue="{Binding ConnectionType}">
                    <ComboBoxItem Content="Tcp"/>
                    <ComboBoxItem Content="WebSocket"/>
                </ComboBox>
            </StackPanel>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Unsubscribe" Command="{Binding UnsubscribeCommand}" Margin="0,0,5,0"/>
            <Button Content="Update" Command="{Binding UpdateCommand}" Margin="0,0,5,0"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
