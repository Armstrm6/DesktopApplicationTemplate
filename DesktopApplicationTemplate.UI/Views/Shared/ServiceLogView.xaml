<UserControl x:Class="DesktopApplicationTemplate.UI.Views.Shared.ServiceLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:services="clr-namespace:DesktopApplicationTemplate.Core.Services;assembly=DesktopApplicationTemplate.Core"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="400">
    <Border Background="#E6E6E6" CornerRadius="5" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Center">
                <TextBlock Text="LOG LEVEL" FontWeight="Bold" Margin="0,0,10,0"/>
                <ComboBox Width="120" SelectedValue="{Binding LogLevelFilter}" SelectedValuePath="Tag">
                    <ComboBoxItem Content="All" Tag="{x:Static services:LogLevel.Debug}" />
                    <ComboBoxItem Content="Debug" Tag="{x:Static services:LogLevel.Debug}" />
                    <ComboBoxItem Content="Warning" Tag="{x:Static services:LogLevel.Warning}" />
                    <ComboBoxItem Content="Error" Tag="{x:Static services:LogLevel.Error}" />
                </ComboBox>
                <Button Content="Update Log" Width="100" Margin="10,0,0,0" Command="{Binding RefreshLogCommand}" Background="#FFFFCC"/>
                <Button Content="Export Log" Width="100" Margin="10,0,0,0" Command="{Binding ExportLogCommand}" Background="#CCCCFF"/>
                <Button Content="Clear Log" Width="100" Margin="10,0,0,0" Command="{Binding ClearLogCommand}" Background="#FFCCCC"/>
            </StackPanel>
            <ListBox Grid.Row="1" ItemsSource="{Binding DisplayLogs}" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" MinHeight="150">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Message}" Foreground="{Binding Color}" TextWrapping="Wrap" Margin="0,0,0,5" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</UserControl>
