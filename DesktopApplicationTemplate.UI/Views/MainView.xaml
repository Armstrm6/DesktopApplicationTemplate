<Window x:Class="DesktopApplicationTemplate.UI.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainView" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Background and border from design -->
        <Border Grid.RowSpan="2"
                Background="{StaticResource Color9Brush}"
                BorderBrush="{StaticResource Color5Brush}"
                CornerRadius="15" />

        <!-- Header area -->
        <Border Grid.Row="0" Margin="4"
                Background="{StaticResource Color1Brush}"
                CornerRadius="14" Padding="8">
            <TextBlock Text="Chapp"
                       Style="{StaticResource TextStyle3}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center" />
        </Border>

        <!-- Content area -->
        <Grid Grid.Row="1" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Placeholder service list to satisfy existing tests -->
            <ListBox x:Name="ServiceList"
                     Grid.Column="0"
                     Width="200"
                     MaxHeight="350"
                     Visibility="Collapsed" />

            <!-- Placeholder frame for dynamic content -->
            <Frame x:Name="ContentFrame"
                   Grid.Column="1"
                   NavigationUIVisibility="Hidden"
                   Visibility="Collapsed" />

            <!-- Primary button from design -->
            <Button x:Name="ChappButton"
                    Grid.ColumnSpan="2"
                    Content="Chapp"
                    Background="{StaticResource Color9Brush}"
                    BorderBrush="{StaticResource Color5Brush}"
                    Padding="20" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Grid>
</Window>
